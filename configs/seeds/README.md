# Seed schedules

Figure and table seed schedules are centralized here.

- `figures/`
  - exact shipped per-alpha seed manifests for `Figure 5`, `Figure S.5`, and `Figure S.6`
- `tables_current.json`
  - current shipped table seed schedules for canonical outputs

`configs/seeds/figures/` is the canonical location for shipped figure manifests.
